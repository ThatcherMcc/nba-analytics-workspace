# Feature Directives

SOPs for building new features (backend and frontend). Use after data pipeline and backend tasks are stable.

- **frontend_data_readiness.md** — What the frontend can rely on: players table (dynamic list), prop_markets (seeded), DNP in player_game_stats (minutes_played). How to fetch player names and when to run pipeline steps.

Place new feature directives here, e.g. `build_prop_prediction_charts.md`.
